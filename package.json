{
  "name": "candy-crush-game",
  "version": "1.0.0",
  "description": "A simple game with comments system using GitHub Discussions",
  "main": "game.js",
  "scripts": {
    "build": "mkdir -p dist && cp comments.html comments.css comments.js dist/",
    "start": "http-server dist"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
} 